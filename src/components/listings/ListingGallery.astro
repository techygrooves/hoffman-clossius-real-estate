---
/**
 * ListingGallery — the photography on a property detail page.
 *
 * Built on a native <dialog>, which is what keeps the JavaScript small:
 * `showModal()` traps focus and closes on Escape without any of it being
 * hand-written. The script only handles moving between images, restoring
 * focus to the thumbnail that opened the lightbox, and touch swipes.
 *
 * With no images — the state today, since there is no photography — it renders
 * placeholder slots and no interactive affordance at all, rather than opening
 * an empty lightbox.
 */
import type { Listing } from '@lib/listings/types';
import MediaSlot from '@components/ui/MediaSlot.astro';
import { cn } from '@lib/cn';

interface Props {
  listing: Listing;
  class?: string;
}

const { listing, class: className } = Astro.props;

const images = listing.images;
const hasImages = images.length > 0;
const addressLabel = listing.address.line1;

/** Desktop mosaic: one large frame plus up to four supporting frames. */
const lead = images[0] ?? null;
const rest = images.slice(1, 5);
const overflow = Math.max(0, images.length - 5);
---

<section
  class={cn('relative', className)}
  aria-label={`Photographs of ${addressLabel}`}
  data-gallery
>
  {
    hasImages ? (
      <>
        <div class="grid gap-2 sm:grid-cols-4 sm:grid-rows-2">
          <button
            type="button"
            data-gallery-open
            data-index="0"
            class="group relative col-span-full row-span-2 overflow-hidden rounded-xs sm:col-span-2"
          >
            <MediaSlot
              src={lead?.url ?? null}
              alt={lead?.alt ?? ''}
              width={lead?.width ?? 1200}
              height={lead?.height ?? 900}
              ratio="4/3"
              loading="eager"
              fetchpriority="high"
              sizes="(min-width: 640px) 50vw, 100vw"
              imgClass="transition-transform duration-700 ease-[var(--ease-editorial)] motion-safe:group-hover:scale-[1.03]"
            />
            <span class="sr-only">Open photograph 1 of {images.length} full screen</span>
          </button>

          {rest.map((image, index) => (
            <button
              type="button"
              data-gallery-open
              data-index={index + 1}
              class="group relative hidden overflow-hidden rounded-xs sm:block"
            >
              <MediaSlot
                src={image.url}
                alt={image.alt}
                width={image.width ?? 600}
                height={image.height ?? 450}
                ratio="4/3"
                sizes="25vw"
                imgClass="transition-transform duration-700 ease-[var(--ease-editorial)] motion-safe:group-hover:scale-[1.05]"
              />
              <span class="sr-only">
                Open photograph {index + 2} of {images.length} full screen
              </span>
              {index === rest.length - 1 && overflow > 0 && (
                <span
                  aria-hidden="true"
                  class="absolute inset-0 flex items-center justify-center bg-evergreen-950/65 font-serif text-2xl text-white"
                >
                  +{overflow}
                </span>
              )}
            </button>
          ))}
        </div>

        <button
          type="button"
          data-gallery-open
          data-index="0"
          class="mt-3 inline-flex h-11 items-center gap-2 rounded-xs border border-line-control px-5 text-sm text-evergreen-800 transition-colors hover:bg-evergreen-50"
        >
          <svg
            class="h-4 w-4"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            aria-hidden="true"
          >
            <rect x="3" y="5" width="18" height="14" rx="1.5" />
            <circle cx="8.5" cy="10" r="1.25" />
            <path d="M21 16l-4.5-4.5L9 19" />
          </svg>
          View all {images.length} photographs
        </button>

        {/*
          Native <dialog>: focus trap and Escape-to-close come from the
          platform, so the script stays under a kilobyte.
        */}
        <dialog
          data-gallery-dialog
          aria-label={`Photographs of ${addressLabel}`}
          class="h-full max-h-none w-full max-w-none bg-evergreen-950/97 p-0 backdrop:bg-evergreen-950/90"
        >
          <div class="flex h-full flex-col" data-surface="dark">
            <div class="flex items-center justify-between gap-4 px-[var(--gutter)] py-4 text-white">
              <p class="text-sm text-white/70">
                <span data-gallery-position>1</span> of {images.length}
              </p>
              <button
                type="button"
                data-gallery-close
                class="flex h-11 w-11 items-center justify-center rounded-xs text-white transition-colors hover:bg-white/10"
              >
                <span class="sr-only">Close photographs</span>
                <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                  <path d="M6 6l12 12M18 6L6 18" />
                </svg>
              </button>
            </div>

            <div class="relative flex flex-1 items-center justify-center overflow-hidden px-2 pb-6 sm:px-16">
              <button
                type="button"
                data-gallery-prev
                class="absolute left-1 z-10 flex h-12 w-12 items-center justify-center rounded-xs text-white transition-colors hover:bg-white/10 sm:left-3"
              >
                <span class="sr-only">Previous photograph</span>
                <svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                  <path d="M15 5l-7 7 7 7" />
                </svg>
              </button>

              <figure class="flex h-full w-full flex-col items-center justify-center gap-3" data-gallery-stage>
                {images.map((image, index) => (
                  <img
                    src={image.url}
                    alt={image.alt}
                    width={image.width ?? 1600}
                    height={image.height ?? 1200}
                    loading="lazy"
                    decoding="async"
                    data-gallery-slide
                    data-index={index}
                    data-caption={image.caption ?? ''}
                    hidden={index !== 0}
                    class="max-h-full max-w-full object-contain"
                  />
                ))}
                <figcaption
                  class="min-h-6 text-center text-sm text-white/70"
                  data-gallery-caption
                >
                  {images[0]?.caption ?? ''}
                </figcaption>
              </figure>

              <button
                type="button"
                data-gallery-next
                class="absolute right-1 z-10 flex h-12 w-12 items-center justify-center rounded-xs text-white transition-colors hover:bg-white/10 sm:right-3"
              >
                <span class="sr-only">Next photograph</span>
                <svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                  <path d="M9 5l7 7-7 7" />
                </svg>
              </button>
            </div>
          </div>
        </dialog>
      </>
    ) : (
      /* No photography yet — a still, honest placeholder, not a dead lightbox. */
      <div class="grid gap-2 sm:grid-cols-4 sm:grid-rows-2">
        <div class="col-span-full row-span-2 overflow-hidden rounded-xs sm:col-span-2">
          <MediaSlot src={null} width={1200} height={900} ratio="4/3" seed={0} loading="eager" />
        </div>
        {[1, 2, 3, 4].map((seed) => (
          <div class="hidden overflow-hidden rounded-xs sm:block">
            <MediaSlot src={null} width={600} height={450} ratio="4/3" seed={seed} marker={false} />
          </div>
        ))}
      </div>
    )
  }
</section>

{hasImages && <script>
  import { initGallery } from '@lib/listings/gallery';
  initGallery();
</script>}
